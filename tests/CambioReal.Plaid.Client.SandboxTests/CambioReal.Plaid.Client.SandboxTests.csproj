<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>CambioReal.Plaid.SandboxTests</RootNamespace>
    <AssemblyName>CambioReal.Plaid.Client.SandboxTests</AssemblyName>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <!--
    Deliberadamente FORA de Plaid.slnx: `dotnet build`/`dotnet test Plaid.slnx` nunca toca este
    projeto, e portanto nenhum pipeline de CI genérico o executa por acidente (goal-loop §2.5:
    "testes de integração sandbox separados, marcados/opt-in, que nunca rodem por padrão em CI").
    Rodar explicitamente: ver README.md deste diretório.
  -->
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="Shouldly" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/CambioReal.Plaid.Client/CambioReal.Plaid.Client.csproj" />
  </ItemGroup>

</Project>
